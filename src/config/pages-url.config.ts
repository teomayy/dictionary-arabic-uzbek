class DASHBOARD {
	private root = '/i'

	HOME = this.root
	SETTINGS = `${this.root}/settings`
	BOOKMARK = `${this.root}/bookmark`
	DESCRIPTION = `${this.root}/description/`
}

export const DASHBOARD_PAGES = new DASHBOARD()
